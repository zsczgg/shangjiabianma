services:
  app:
    environment:
      INTEGRATION_API_KEY: ${INTEGRATION_API_KEY:?请在 .env 中配置 INTEGRATION_API_KEY}
      API_KEY_ENCRYPTION_SECRET: ${API_KEY_ENCRYPTION_SECRET:?请在 .env 中配置 API_KEY_ENCRYPTION_SECRET}
